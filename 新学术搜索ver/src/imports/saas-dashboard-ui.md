Overall UI Style & Layout:

Design a modern, professional, card-based SaaS dashboard interface.

Focus strictly on the main right-side workspace panel.

Ensure all components use slightly rounded corners and a clean white/light-gray color palette, contrasting with one dark-themed account balance card.

Structure (Top to Bottom):

1. Main Workspace Header:

A bold title at the top left of the workspace: WisPaper.

2. Account Balance Card:

A large, dark-themed (e.g., black or dark navy) horizontal card.

Top Left: Title "账户余额" (Account Balance) with an associated icon.

Top Right: A button labeled "充值" (Recharge).

Inside (Two Columns):

Column 1: Text "CREDITS 余额" (CREDITS Balance), prominent figure 1,250 Credits, and a note "将于 2024-03-31 到期" (Expires on 2024-03-31).

Column 2: Text "API 余额" (API Balance), prominent figure $25.00 USD, and a note "用于模型 API 接口调用" (For model API interface calls).

Descriptive line below the card: "用于 API调用、GPU部署以及大模型Agent调用".

3. Process Stepper Bar (Updated for Idea Discovery):

A horizontal progress bar with four distinct steps, each with a circular icon and text.

Step 1 (Active/Highlighted): Icon showing a lightbulb or question mark. Text: 输入主题 (Topic Input).

Step 2: Icon showing a refinement tool. Text: 优化指令 (Refine Instruction).

Step 3: Icon showing a document with a plus sign. Text: 生成大纲 (Generate Outline).

Step 4: Icon showing a report with a magnifying glass. Text: 进入复现 (Go to Reproduction).

4. Main Feature Input Card: 'Idea Discovery Agent'

A large, clean white card, replicating the structure of the input area.

Feature Title: Idea Discovery Agent.

Subtitle: "通过宽泛的主题检索最新文献，快速发现具有创新性的论文Idea" (Help you quickly discover innovative paper ideas from broad topics).

Step 1 Section (Main Input):

Section Title: 1. 输入科研主题 (Topic).

A large, dash-bordered input box.

Centrally inside: Text input field. Placeholder text: "请输入你感兴趣的主题（例如：大语言模型在医疗问诊中的幻觉问题）" (Please enter the Topic you are interested in (e.g., Hallucinations of large language models in medical consultation)).

Helpful tips/Prompt examples list below the input field in a lighter-gray panel:

• 多模态模型在自动驾驶中的长尾问题

• 基于对比学习的小样本图像分类方法

• 用于推荐系统的图神经网络局限性

Step 2 Section (Optional Instruction):

Section Title: 2. 优化指令（可选）.

Multi-line text input field. Placeholder text: "可以输入特定的侧重点（例如：更关注缓解策略而非评测方法）..." (Click here to enter...).

Bottom Action Bar (within card):

Left: A "取消" (Cancel) text link.

Right: A primary action button with a blue background and a star icon. Button text: 检索并生成Idea大纲 (Retrieve & Generate Idea Outline).

5. Bottom Metrics Row:

A horizontal row of three smaller, card-like metric widgets.

Widget 1: Title "平均检索时间" (Average retrieval time). Value: 2-4分钟 (2-4 minutes).

Widget 2: Title "生成质量评分" (Generation quality score). Value: High.

Widget 3: Title "模型可用性" (Model Availability). Value: 是 (Yes).

6. Output State Container (Add a placeholder for the output ideas):

Below the metrics, add a placeholder section titled: Candidate Ideas (Structural Outlines).

Describe it as showing 1-3 horizontal or vertical cards.

Each card should feature the structured information: Idea Title, Problem (Research Gap), Method (Proposed Approach), Innovation (Novelty), and Reference Papers (with links).

Crucially, each candidate idea card needs an action button on the bottom: [ 🚀 进入论文复现与验证 ] (Enter paper reproduction and verification).